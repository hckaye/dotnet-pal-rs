[package]
name = "dotnet-pal-memfs"
version = "0.7.0"
edition = "2021"
rust-version = "1.98"
description = "In-memory file system provider (no_std + alloc) for dotnet-pal-rs ports without storage hardware"
license = "MIT"

[dependencies]
dotnet-pal-rs = { path = "../..", default-features = false }
